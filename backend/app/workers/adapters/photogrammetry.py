from __future__ import annotations
from pathlib import Path

def reconstruct_from_images(image_dir: Path, out_glb: Path) -> None:
    # Placeholder: replace with COLMAP/NeRF/NS/RealityCapture pipeline as you choose
    out_glb.write_bytes(b"GLB_PLACEHOLDER_FROM_SCAN")
